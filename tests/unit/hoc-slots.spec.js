import { mount } from '@vue/test-utils'

describe('HOC Behaviour with slots', () => {
  it('by default, named slots are passed through correctly', () => {
    mount // something
  })

  it('when updateSlotContext = false, all slot nodes show up in default slot', () => {})
})
